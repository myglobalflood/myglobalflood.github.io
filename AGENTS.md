# Project publishing context

- The primary public site is GitHub Pages from `myglobalflood/myglobalflood.github.io`.
- Keep `Grups666/AHSLab` only as the previous preview until the teacher-owned site is verified.
- Preserve the existing ChatGPT Site project defined in `.openai/hosting.json` as a secondary preview and keep its project ID unchanged.
- Do not use the retired `Grups666/WJGroup` repository for this project.
- A later production handoff may move to institutional hosting or another explicitly confirmed destination.
